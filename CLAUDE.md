# CLAUDE.md

우리 가족이 함께 쓰는 **가족 생활 관리 웹앱**입니다. 이 문서는 이 저장소에서 작업할 때 따라야 할 규칙과 맥락을 담고 있습니다.

> 현재 상태: **1차 구현 완료.** 네 가지 기능이 모두 동작하고, 데이터는 브라우저
> 안(localStorage)에 저장됩니다. 백엔드(Supabase)는 아직 붙이지 않았습니다.
> 자세한 건 아래 2번 항목을 보세요.

## 1. 무엇을 만드는가

가족 구성원(부모 + 자녀)이 한 공간에서 일정과 할 일을 공유하고, 아이들의 숙제와 행동을 포인트로 관리하는 웹앱입니다.

핵심 기능 4가지:

| 기능 | 설명 |
| --- | --- |
| **일정 관리** | 가족 공용 캘린더. 개인 일정 / 가족 전체 일정 구분. 월·주 단위 보기 |
| **할일 공유** | 집안일 등 할 일을 만들고 담당자를 지정. 완료 체크 |
| **숙제 관리** | 아이별 숙제 등록, 마감일, 제출/확인 상태. 부모가 확인 처리. 학원 숙제 사진에서 글자를 읽어 한 번에 등록 |
| **칭찬 / 벌점** | 잘한 일은 +점, 규칙 위반은 -점. 아이별 누적 포인트와 이력 |

### 설계 의도 (중요)

- **아이도 쓰는 앱이다.** 화면은 크고 단순하게. 어려운 용어 금지. 저학년도 혼자 쓸 수 있어야 함.
- **칭찬이 주인공, 벌점은 조연.** 벌점 UI를 강조하거나 빨갛게 도배하지 말 것. 아이가 앱을 열었을 때 기분이 나빠지면 실패한 설계입니다.
- **가족 한 팀만 쓴다.** 여러 가족을 받는 SaaS가 아닙니다. 멀티테넌시, 결제, 조직 관리 같은 건 넣지 마세요.
- **작게 유지한다.** 기능 추가보다 "매일 켜게 되는가"가 중요합니다.

## 2. 기술 스택

- **프레임워크**: React 19 + TypeScript (strict)
- **빌드**: Vite
- **스타일**: Tailwind CSS
- **라우팅**: React Router
- **백엔드/DB**: **아직 없음.** `src/lib/db.ts` 가 localStorage 에 저장합니다
- **패키지 매니저**: npm

### 저장은 지금 어떻게 되고 있나

계정도 키도 없이 바로 쓸 수 있도록, 1차 구현은 브라우저 저장소만 씁니다.

- 장점: 설치하고 `npm run dev` 만 하면 끝
- 한계: **기기끼리 공유되지 않습니다.** 엄마 폰과 거실 PC가 서로 다른 데이터를 봅니다
- 한계: 브라우저 사이트 데이터를 지우면 함께 사라집니다 (설정 화면에 백업 기능이 있습니다)

### 백엔드를 붙일 때

가족이 각자 기기에서 같은 데이터를 보려면 서버가 필요합니다. Supabase 무료 티어를
쓰는 걸 전제로 설계해 뒀습니다.

**바꿔야 하는 곳은 `src/lib/db.ts` 하나뿐입니다.** 화면 코드는 전부 이 파일이
내보내는 함수(`addTask`, `confirmTask`, `useDb` …)만 부르고 있어서, 그 함수들의
본문을 Supabase 쿼리로 갈아끼우면 됩니다.

순서:

1. Supabase 프로젝트를 만들고 `.env.local` 에 URL/anon key 를 넣는다
2. 3번 항목의 스키마대로 테이블을 만든다 (컬럼은 snake_case)
3. 4번 항목의 권한 표를 **RLS 정책으로** 옮긴다
4. `db.ts` 의 함수 본문을 바꾸고, `useDb` 를 Realtime 구독으로 교체한다

> Supabase 대신 Firebase 를 써도 무방합니다. 다만 한 번 정하면 바꾸지 마세요.
> 실제로 설치·설정하기 전에 사용자에게 한 번 확인받으세요.

## 3. 데이터 모델

테이블 6개면 충분합니다. 필요해지기 전에 늘리지 마세요.

```
family          가족 (한 행만 존재)
  id, name

member          구성원
  id, family_id, name, role('parent'|'child'), color, birth_date, avatar_emoji

event           일정
  id, family_id, title, starts_at, ends_at, all_day,
  owner_id(null이면 가족 전체 일정), memo

task            할일 + 숙제 (같은 테이블, kind로 구분)
  id, family_id, title, kind('chore'|'homework'),
  assignee_id, due_at, status('todo'|'done'|'confirmed'),
  subject(숙제 과목, chore면 null), reward_points

point_entry     칭찬 / 벌점 이력
  id, family_id, member_id, points(양수=칭찬, 음수=벌점),
  reason, given_by(부모 member_id), created_at, task_id(null 가능)

reward          포인트로 바꾸는 보상
  id, family_id, title, cost_points, active
```

### 규칙

- **할일과 숙제는 같은 테이블**입니다. `kind` 하나로 구분하세요. 테이블을 쪼개면 "오늘 할 일 전체 보기"가 복잡해집니다.
- **포인트 총합은 저장하지 않습니다.** `point_entry`를 합산해서 구하세요. 이력과 총점이 어긋나는 사고를 막기 위함입니다.
- **삭제는 하드 삭제.** 가족 앱에 감사 로그는 과합니다. 단, `point_entry`만은 지우지 말고 취소용 반대 부호 항목을 추가하세요 (아이와 분쟁이 생겼을 때 기록이 남아야 함).
- **숙제를 완료하면 자동으로 포인트가 붙지 않습니다.** 부모가 `confirmed`로 바꿀 때 `reward_points`만큼 `point_entry`가 생깁니다.
- **사진은 어디에도 저장하지 않습니다.** 숙제 사진은 글자만 뽑아내고 버립니다. 테이블에 이미지 컬럼을 추가하지 마세요 (아래 참고).

## 4. 권한

로그인은 가족 구성원 선택 + 간단한 PIN 방식입니다. 이메일/비밀번호는 아이들에게 과합니다.

| 동작 | 부모 | 자녀 |
| --- | --- | --- |
| 일정 보기 | O | O |
| 일정 만들기/수정 | O | 본인 것만 |
| 할일·숙제 만들기 | O | X |
| 할일·숙제 완료 체크 | O | 본인 담당만 |
| 완료 확인(confirmed) | O | X |
| 칭찬/벌점 주기 | O | X |
| 포인트 이력 보기 | 전원 | 본인 것만 |
| 구성원 관리 | O | X |

규칙은 `src/lib/permissions.ts` 한 곳에 모여 있습니다. 버튼을 숨길 때도, 실제로
데이터를 바꾸기 직전에도 같은 함수를 쓰세요.

백엔드를 붙이면 **똑같은 규칙을 Supabase RLS 로 다시 한 번** 강제해야 합니다.
프론트엔드에서 버튼만 숨기는 것은 보안이 아닙니다.

## 5. 디렉터리 구조

```
src/
  pages/          라우트 단위 화면 (Login, Calendar, Tasks, Homework, Points, Settings)
  components/     재사용 UI (Layout, Modal, Avatar, EmptyState)
  features/
    auth.tsx      로그인 세션 (구성원 선택 + PIN)
    events/       일정 폼
    tasks/        할일·숙제 (TaskBoard / TaskRow / TaskForm)
                  + ImportFromPhoto — 사진에서 숙제 가져오기
    points/       칭찬·벌점 주기
    members/      구성원 폼
  lib/
    db.ts         데이터 계층 — 저장소를 바꿀 때 여기만 고칩니다
    date.ts       Asia/Seoul 기준 날짜 계산
    permissions.ts 누가 무엇을 할 수 있는지
    ocr.ts        사진에서 글자 읽기 (tesseract.js)
    seed.ts       처음 켰을 때 들어가는 예시 데이터
  types/          공용 타입 정의
```

- 도메인 로직은 `features/` 안에, 화면 조립만 `pages/`에서 합니다.
- **할일과 숙제는 같은 컴포넌트(`TaskBoard`)를 씁니다.** 숙제 화면은
  `groupByMember` 를 켜서 아이별로 묶어 보여 주는 것만 다릅니다.
- 컴포넌트가 200줄을 넘으면 쪼갤 때가 된 겁니다.

## 6. 사진에서 숙제 가져오기

학원 숙제가 사진으로 오는 경우가 많아서, 사진의 글자를 읽어 숙제 항목으로
만들어 줍니다. `숙제` 탭의 **📷 사진** 버튼 → `features/tasks/ImportFromPhoto.tsx`.

인식은 `lib/ocr.ts` 에서 **tesseract.js** 로 브라우저 안에서 처리합니다.

### 지켜야 할 것

- **사진을 저장하지 마세요.** 글자만 뽑고 버립니다. 파일도, IndexedDB 도,
  서버도 아닙니다. 아이 숙제 사진이 기기 밖으로 나가지 않는다는 게 이 설계의
  핵심입니다. "나중에 다시 보게 사진도 남기자"는 요청이 오면 저장 위치와
  용량부터 다시 이야기하세요 (사진 한 장이 localStorage 한도를 넘깁니다).
- **인식 결과를 그대로 저장하지 마세요.** OCR 은 항상 틀립니다. 사람이
  텍스트를 보고 고치는 단계를 반드시 거쳐야 합니다. 지금 흐름은
  `사진 → 글자 → 사람이 확인/수정 → 등록` 입니다. 이 가운데 단계를 빼지 마세요.
- 인쇄된 글씨는 잘 읽고 **손글씨는 많이 틀립니다.** 정확도를 올리려고
  전처리를 덧붙이기 전에, 사람이 고치는 화면을 더 편하게 만드는 쪽을 먼저
  검토하세요.
- 자르는 규칙은 `toHomeworkLines()` 한 곳에 있습니다. 한 줄에 하나로만 자르고,
  앞머리 기호(`1.`, `-`, `•`)를 뗍니다. 여기서 더 똑똑해지려 하지 마세요.

### 알아 둘 것

- 한글·영어 인식 데이터(약 10MB)를 처음 한 번 CDN 에서 받습니다. 그 뒤로는
  브라우저가 캐시합니다. **즉, 첫 사용에는 인터넷이 필요합니다.**
- 사진 없이 텍스트를 붙여넣어 여러 개를 한 번에 등록하는 길도 열어 뒀습니다.
  OCR 이 안 될 때의 탈출구이니 없애지 마세요.

## 7. 코딩 규칙

- **한국어 UI**. 화면에 보이는 모든 문구는 한국어. 코드·주석·커밋 메시지는 영어 또는 한국어 중 파일 내에서 일관되게.
- **날짜는 항상 한국 시간(Asia/Seoul) 기준**으로 표시합니다. DB에는 UTC로 저장하고 표시할 때 변환하세요.
- `any` 금지. 타입을 모르겠으면 `unknown`으로 두고 좁히세요.
- 상태 관리 라이브러리(Redux, Zustand 등)를 추가하지 마세요. 이 규모에서는 `useState` + Supabase 쿼리로 충분합니다.
- 새 npm 패키지를 추가하기 전에 먼저 물어보세요.

## 8. 명령어

```bash
npm install      # 의존성 설치
npm run dev      # 개발 서버 (http://localhost:5173)
npm run build    # 타입 체크 + 프로덕션 빌드
npm run preview  # 빌드 결과 미리보기
npm run lint     # 린트
```

작업을 끝내기 전에 `npm run build` 와 `npm run lint` 가 모두 통과하는지 확인하세요.

> **사내망 참고**: 회사 프록시가 TLS 를 중간에서 다시 서명하는데 그 루트 인증서를
> Node 24 가 거부합니다(`CA certificate key too weak`). 그래서 `.npmrc` 에
> `strict-ssl=false` 를 넣어 두었습니다.
>
> 이 파일은 **git 에 올리지 않습니다**(`.gitignore` 에 있음). 이 PC 전용
> 우회 설정일 뿐이고, 집 네트워크에서는 필요 없을 뿐더러 검증을 끄는 건
> 기본값으로 둘 만한 설정이 아니기 때문입니다. 다른 사내망 PC 에서 받아서
> `npm install` 이 인증서 오류로 막히면 그때 같은 내용으로 새로 만드세요.

## 9. 환경 변수

지금은 환경 변수 없이도 앱이 돌아갑니다. Supabase 를 붙일 때 `.env.local` 에
아래 두 개를 채우세요 (git 에 커밋하지 말 것):

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

`.env.example`에는 키 이름만 두고 값은 비워 두세요.

## 10. 작업할 때 주의할 점

- 이 저장소에는 **실제 가족 구성원의 이름, 사진, 일정**이 들어갑니다. 테스트 데이터나 로그를 외부로 보내지 마세요.
- 마이그레이션이나 시드 스크립트로 기존 데이터를 지우기 전에 반드시 확인받으세요.
- 기능을 제안할 때는 "이걸 우리 아이가 쓸까?"를 먼저 따져보세요. 안 쓸 것 같으면 만들지 마세요.
