<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 안드로이드 13+ 에서 알림을 띄우려면 앱이 이 권한을 가져야 합니다 (크롬이 앱 이름으로 알림을 냄) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- 이 앱이 어느 사이트의 것인지. https://lmjpt.github.io/.well-known/assetlinks.json 과 짝입니다. -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <!-- 웹앱을 전체 화면으로 여는 진입점 (Trusted Web Activity) -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://lmjpt.github.io/family-hub/" />
            <!--
              항상 크롬으로 엽니다. 기본 브라우저가 삼성 인터넷이면 앱이 그걸 쓰게 되는데,
              알림을 '우리집' 이름으로 넘겨 주는(위임) 기능은 크롬만 지원해서 알림이
              '인터넷' 이름으로 옵니다. 크롬이 없는 폰이면 라이브러리가 안내 창을 띄웁니다.
            -->
            <meta-data
                android:name="android.support.customtabs.trusted.LAUNCHING_BROWSER"
                android:value="com.android.chrome" />
            <meta-data
                android:name="android.support.customtabs.trusted.LAUNCHING_BROWSER_NAME"
                android:value="Chrome" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/cream" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/cream" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 위젯을 누르거나 우리 사이트 링크를 열면 이 앱으로 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="lmjpt.github.io"
                    android:pathPrefix="/family-hub" />
            </intent-filter>
        </activity>

        <!--
          알림 위임. 이게 있으면 크롬이 웹 푸시 알림을 '크롬' 이 아니라 '우리집' 앱 이름으로
          띄우고, 폰 설정 → 알림 목록에도 우리집이 나타납니다. 없으면 알림 권한이 크롬의
          사이트 설정에 숨어 있어서 가족이 찾을 수 없습니다.
        -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.DelegationService"
            android:enabled="true"
            android:exported="true">
            <!-- 알림 상태 표시줄에 쓸 작은 아이콘. 없으면 알림이 표시되지 않을 수 있습니다. -->
            <meta-data
                android:name="android.support.customtabs.trusted.SMALL_ICON"
                android:resource="@drawable/ic_notification" />
            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <!--
          웹에서 알림 권한을 요청하면 크롬이 이 투명 화면을 띄워 '우리집' 앱 권한을 묻습니다.
          (Bubblewrap 이 알림을 켤 때 넣는 선언과 같습니다.) 이게 없으면 크롬 사이트 권한으로 되돌아갑니다.
        -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.NotificationPermissionRequestActivity"
            android:exported="false" />

        <!-- 웹 화면의 '위젯 연결' 버튼이 familyhub://widget?token=... 로 열어 주는 곳 -->
        <activity
            android:name=".WidgetLinkActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="familyhub"
                    android:host="widget" />
            </intent-filter>
        </activity>

        <!-- 바탕화면 위젯: 오늘 일정과 마감 -->
        <receiver
            android:name=".TodayWidget"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_widget_info" />
        </receiver>
    </application>
</manifest>
