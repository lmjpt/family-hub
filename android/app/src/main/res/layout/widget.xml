<?xml version="1.0" encoding="utf-8"?>
<!-- 위젯 전체가 버튼입니다. 누르면 앱이 열립니다. 줄은 최대 6개, 넘치면 마지막 줄에 '+N개 더'. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="10dp">

    <TextView
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/app_name"
        android:textColor="@color/muted"
        android:textSize="12sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/line1"
        style="@style/WidgetLine"
        android:text="불러오는 중…" />
    <TextView android:id="@+id/line2" style="@style/WidgetLine" android:visibility="gone" />
    <TextView android:id="@+id/line3" style="@style/WidgetLine" android:visibility="gone" />
    <TextView android:id="@+id/line4" style="@style/WidgetLine" android:visibility="gone" />
    <TextView android:id="@+id/line5" style="@style/WidgetLine" android:visibility="gone" />
    <TextView android:id="@+id/line6" style="@style/WidgetLine" android:visibility="gone" />

    <!-- 남는 세로 공간을 먹는 빈 칸. 위젯(RemoteViews)에서는 plain View 를 쓸 수 없어서
         FrameLayout 을 씁니다. View 를 넣으면 런처가 위젯을 그리지 못해 빈 상자만 보입니다. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <TextView
        android:id="@+id/footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/muted"
        android:textSize="10sp" />
</LinearLayout>
